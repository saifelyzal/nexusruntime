<!-- NEXUS AI Gateway logo mark. The parent owns sizing (Sidebar's
     `.sidebar-logo` wrapper), the image fills its container. -->
<script>
  import { gomodelPath } from "$lib/api/paths.js";
</script>

<img src={gomodelPath("/admin/static/logo.png")} alt="" aria-hidden="true" />
