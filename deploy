nexus deploy source --repo https://github.com/saifelyzal/nexusruntime.git \
--name aigateway \
--services postgresql,redis \
--provider docker \
--json \
--framework go \
--no-health-check no \
--env-file /Users/ihope/build/nexusruntime/.env \
--repo-secret gitrepo \
--dockerfile "$(cat /Users/ihope/build/nexusruntime/Dockerfile.nexus)"
